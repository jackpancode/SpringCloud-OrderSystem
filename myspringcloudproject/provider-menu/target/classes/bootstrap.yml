server:
  port: 8020

spring:
  application:
    name: menu-8020
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: dev
        namespace: e33fe902-2f13-43ee-94be-b91d18980e40
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
