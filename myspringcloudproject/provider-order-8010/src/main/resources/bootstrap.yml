server:
  port: 8010

spring:
  application:
    name: order-8010
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: dev
        namespace: 19bf5508-99be-4760-98b6-bb9ada675440
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
